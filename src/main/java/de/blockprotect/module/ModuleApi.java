package de.blockprotect.module;

/** Versioned contract between the permanent core and reloadable modules. */
public final class ModuleApi {
    public static final int VERSION = 1;

    private ModuleApi() {
    }
}
